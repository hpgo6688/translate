推荐拆法
只起一个 change,建议名字 add-mvp-web-translation,范围:

- 仅客户端(无服务端,Free 模式)
- WXT + React 项目骨架
- DOM 段落识别 + 双语注入 + Shadow DOM
- IndexedDB 段落缓存
- Provider 抽象层 + Google + DeepL 实现
- service worker 调度 + 限流
- popup(开关 + 切语言 + 切 provider)
- options(默认语言 + Key 管理 + 显示样式)



P1 起再开 add-saas-gateway、add-ai-providers 等 change,与 Go 服务端引入同步。